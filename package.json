{
  "name": "bursary-system",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node generate-pages.js && node scripts/generate-homepage.js && node generate-sitemap.js",
    "generate": "npm run build",
    "sitemap": "node generate-sitemap.js"
  }
}
